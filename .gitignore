node_modules
dist
coverage

# Sourcemaps apartados por `npm run build:hosting`: no se suben al hosting
# publico, pero se guardan en local para poder mapear un stack a mano.
.sourcemaps/
.DS_Store
.env
.env.local
.env.*.local
*.tsbuildinfo
vite.config.d.ts
supabase/.temp/

# Test artifacts (Playwright screenshots/traces)
tmp/
test-results/
playwright-report/
reports/

# Design source assets (no versionar imágenes pesadas de diseño)
rediseno/
rediseño/
logosPasareladePago/
